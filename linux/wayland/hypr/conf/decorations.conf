decoration {
    rounding = 5
    rounding_power = 5
    blur {
        enabled = 1
        size = 8
        passes = 2
        new_optimizations = 1
        xray = 1
    }

    # These are not considered as windows so we need to do it like this
    # blurls = waybar
    blurls = lockscreen
}
